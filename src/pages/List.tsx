import React from 'react';

import { Button } from '../components/UI';

const List: React.FC = () => {
  return (
    <div>
      <h1>List</h1>
      <Button>Создать заявку</Button>
    </div>
  );
};

export default List;
